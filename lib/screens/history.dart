import 'package:flutter/material.dart';

// Ventana del historial de productos escaneados
class History extends StatelessWidget {
  const History({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Historial"),
      ),
    );
  }
  
}